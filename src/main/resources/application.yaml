spring:
  application:
    name: ecommerce

  datasource:
    url: jdbc:mysql://localhost:3306/ecommerce
    username: root
    password: root

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update


  flyway:
    enabled: true